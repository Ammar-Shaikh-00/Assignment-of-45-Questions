let friends = ["Alice", "Bob", "Charlie"];

// Intentionally accessing an index that doesn't exist to cause an error
console.log(friends[3]); 

// Correcting the error by accessing a valid index
console.log(friends[2]); 
